# Security

`site2voice` fetches public URLs and reads local files passed by the user. It
does not execute page JavaScript and does not require credentials.

Report security issues privately through GitHub security advisories if enabled,
or by opening a minimal issue that does not include exploit details.

Do not use `site2voice` against private, authenticated, or sensitive pages
unless you are comfortable storing derived text metrics locally.

